# migrations/

Auto-generated SQL migration files managed by [Drizzle Kit](https://orm.drizzle.team/kit-docs/overview).

Do not edit these files manually. Instead:

1. Modify `src/schema.ts`
2. Run `bunx drizzle-kit generate` to generate a new migration
3. Run `bun run migrate` to apply it