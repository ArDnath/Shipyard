# migrations/

SQL migration files generated by Drizzle Kit.

Run migrations:
```bash
bun run --filter '@deploystack/db' migrate
```

Generate a new migration after schema changes:
```bash
bunx drizzle-kit generate
```
